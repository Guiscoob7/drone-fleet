FROM golang:1.21-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN go build -o /drone ./cmd/drone
FROM alpine:latest
COPY --from=builder /drone /drone
ENTRYPOINT ["/drone"]